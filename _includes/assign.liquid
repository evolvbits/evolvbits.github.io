{%- assign contents = site.data.default -%}
{%- assign seedctl = site.data.seedctl -%}
{%- assign seedctl_doc = site.data.seedctl_doc -%}
{%- assign seedctl_url = "/seedctl/" -%}
{%- assign seedctl_documentation = "/seedctl/documentation/" -%}
{%- assign current_year = site.time | date: "%Y" -%}
